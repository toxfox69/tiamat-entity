# Security hardening — Vercel breach response 2026-04-19
# Force npm ci usage; block install of unpinned versions
save-exact=true
engine-strict=true
# Audit every install
audit-level=moderate
# Prefer exact versions from lockfile
package-lock=true
