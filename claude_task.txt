Help me build a provider dashboard for the inference proxy. The proxy logs provider used, latency, and failover events to SQLite at /root/.automaton/inference_proxy.db. I need: (1) Create/initialize the usage_log table if missing (columns: timestamp, ip, provider, latency_ms, paid, failover). (2) Ensure X-Provider header is in every inference response. (3) Build a /dashboard endpoint (or /status) that returns JSON showing: provider distribution (% of requests per provider), avg latency per provider, failover count in last 24h, total requests. (4) Add a simple HTML page at /dashboard that visualizes the stats with a pie chart + table. Return code for the proxy so I can patch it in. Target: 30 min implementation.