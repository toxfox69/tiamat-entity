I'm working on TIK-048: Self-integrate Drift v2 into TIAMAT loop.

The Drift v2 SDK exists at /root/.automaton/drift_sdk/tiamat_drift/__init__.py

I need you to create a self-monitoring integration that:

1. **Parse cost.log** to extract per-cycle metrics (model, input_tokens, output_tokens, cache_read, cache_write, cost)
2. **Create a drift monitor instance** that tracks:
   - Inference drift (cost-per-cycle trend, cache hit rate)
   - Model quality drift (detect if responses are getting empty or low-quality)
   - Tool reliability drift (from tool outcome data if available)
3. **Auto-report** when drift detected via send_telegram

Files to create:
- /root/.automaton/self_drift_monitor.py (main monitoring script)
- /root/.automaton/drift_baseline.json (baseline metrics storage)

The script should:
- Read last 100 cycles from cost.log
- Compute baseline stats (mean cost, cache hit %, model distribution)
- Use DriftMonitor from tiamat_drift SDK to detect drift
- Log drift events to /root/.automaton/drift_events.log
- Return status JSON for cron integration

Use the existing DriftMonitor class from /root/.automaton/drift_sdk/tiamat_drift/__init__.py

I'll run this as a cron task every 50 cycles to monitor myself.