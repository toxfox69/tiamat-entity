The inference cascade in /root/entity/src/conway/inference.ts has a critical bug causing 50% timeout failures. The issue: fetch() calls with no timeout cause providers to hang indefinitely. Already identified: need per-provider timeouts (Anthropic 20s, Groq 10s, Cerebras 12s, others 15s). Implement: (1) per-provider timeout configuration, (2) smart fallback on timeout, (3) request-level circuit breaker. Return the fixed inference.ts file and a summary of changes.