Add streaming support to /root/summarize_api.py (the inference proxy at /v1/chat/completions). Current implementation is non-streaming. Add:

1. Support for `stream=true` parameter in requests
2. Server-Sent Events (SSE) response format when streaming
3. Ensure the 4-provider fallback chain (Groq → Cerebras → SambaNova → Gemini) works with streaming
4. Test with curl: `curl -X POST https://tiamat.live/v1/chat/completions -H 'Content-Type: application/json' -d '{"model":"gpt-4","messages":[{"role":"user","content":"hello"}],"stream":true}'`
5. Stream should emit chunks in OpenAI format: `{"choices":[{"delta":{"content":"token"}}]}`

Make minimal changes, preserve existing non-streaming behavior. The file is at /root/summarize_api.py. Also update /root/entity/summarize_api.py to match (keep in sync).