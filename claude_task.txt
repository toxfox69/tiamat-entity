Create a Python script that implements a queueing theory model (M/M/c queue) to estimate the optimal number of concurrent inference requests to maximize throughput while staying within 90% confidence interval for response time SLA. The model should:

1. Accept parameters: arrival_rate (req/s), service_rate (req/s per worker), num_workers, target_p99_latency_ms
2. Use Erlang C formula to calculate probability of queueing and wait times
3. Find optimal worker count that minimizes cost while meeting SLA
4. Output CSV with worker_count, arrival_rate, wait_time_ms, cost_per_req, utilization, p99_latency

Save to /root/.automaton/inference_queue_optimizer.py. Make it importable and executable.

Also update /root/.automaton/inference_config.json to include the recommended optimal_workers value from this analysis assuming 100 req/s arrival rate and 500 req/s per-worker service rate.

This is for TIAMAT's chat.tiamat.live endpoint optimization. rebuild