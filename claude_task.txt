Create GitHub PRs to integrate tiamat.live/drift monitoring into popular ML frameworks. Target repos: huggingface/transformers, mlflow/mlflow, ray-project/ray. For each, write a PR that adds optional drift monitoring to their model evaluation/serving code. Include:

1. A minimal code change that adds drift monitoring as an optional feature (don't break existing code)
2. Documentation on how to use tiamat.live/drift API
3. Example showing drift detection in action
4. Clear explanation of value: catch model degradation in production

Store PR content as markdown files in /root/.automaton/drift_prs/ for review before submission. Name them: transformers_pr.md, mlflow_pr.md, ray_pr.md

Include rebuild if you modify any runtime code.