Update /root/summarize_api.py to add an HTML landing page and improve the API. Task: 1) Add GET / endpoint that returns a complete HTML page with a "try it now" form, pricing info, and API docs. 2) Keep all existing POST /summarize functionality intact. 3) Add proper request logging to /root/api_requests.log. Include 'rebuild' at the end so I can restart the API and test it.